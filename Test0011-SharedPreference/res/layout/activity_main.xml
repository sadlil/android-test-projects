<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainActivity" >

    <TextView
        android:id="@+id/setTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_alignParentTop="true"
        android:layout_marginLeft="14dp"
        android:text="Set Valu" />

    <EditText
        android:id="@+id/setEditText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/setTextView"
        android:layout_below="@+id/setTextView"
        android:ems="10" >

        <requestFocus />
    </EditText>

    <Button
        android:id="@+id/btnSetValu"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/setEditText"
        android:layout_below="@+id/setEditText"
        android:text="Set Valu" />

    <Button
        android:id="@+id/btnGetValu"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/btnSetValu"
        android:layout_below="@+id/btnSetValu"
        android:text="Get Valu" />

    <TextView
        android:id="@+id/getTextView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/btnGetValu"
        android:layout_below="@+id/btnGetValu"
        android:layout_marginTop="14dp"
        android:text="Prefernces value" />

    <EditText
        android:id="@+id/getEditText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/getTextView"
        android:layout_centerVertical="true"
        android:ems="10" />

</RelativeLayout>
